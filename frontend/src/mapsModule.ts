// Native entry point — re-exports from the real react-native-maps package.
// On web, Metro resolves mapsModule.web.ts instead and never touches this file.
export { default, Marker } from 'react-native-maps';
export type { Region } from 'react-native-maps';
